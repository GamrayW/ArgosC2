server:
  host: "0.0.0.0"
  port: 5000


postgres_server:
  host: "127.0.0.1"
  port: 5432
  db_name: "argos_db"
  db_user: "argos"
  db_pass: "changeme"

agents_path: "../agents/"


operators:
  - username: "admin"
    password: "admin"

listeners:
  - name: "coucou"
    api_key: "123456789"

logging:
  enable_logging: true
  log_file: "/tmp/logfile.log"
  log_level: "INFO"  # PEUT ETRE DES TRUCS DU STYLE 'DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL'
